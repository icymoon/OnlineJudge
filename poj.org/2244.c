#include <stdio.h>

int main(int argc, char * argv[]) {
    int n, i, j, k, count;
    int num[150];
    while (scanf("%d", n) == 1) {
        m = 2;
        count = 0;
        for (i = 0; i < n; i ++) {
            num[i] = 1;
        }

        while (count < n) {
            
        }
    }
    return 0;
}
